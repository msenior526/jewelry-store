class Cart {
    constructor(user_id) {
        this.userId = user_id
    }

    static addItemToCart(item) {
        alert('Added to cart!')
    }

    set products(prod) {
        // debugger
    }
}